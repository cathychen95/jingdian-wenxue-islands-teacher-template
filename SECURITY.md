# 安全與學生資料提醒

## 不可上傳或分享的內容

- Cloudflare API Token、Global API Key 或其他憑證。
- `.dev.vars`、`.env` 或任何含真實密碼的檔案。
- 已匯出的學生名單、題庫答案、作答紀錄或 D1 備份。
- 真實教師帳號與密碼。

## 教師部署後

1. 首次登入後，可到「帳號安全」更改教師帳號與密碼。
2. 密碼至少為 6 位純數字，且不應使用生日、班級代碼或連續數字。
3. 學生 PIN 應由教師個別提供，不要公開張貼整班 PIN 表。
4. 僅蒐集教學必要資料，並依學校個資政策保存及刪除。
5. 不再需要的 Cloudflare API Token 應立即撤銷。

## 學生登入階段

- 每位學生同時只保留一個有效登入階段；新裝置登入會撤銷舊裝置的登入階段。
- 舊裝置會在下一次操作、回到頁面或最遲約 30 秒內回到登入頁。
- 這項保護只影響登入憑證，不會刪除學生的進度、配件或作答紀錄。
- 若多人需要同時體驗，請建立不同座號與 PIN，不要共用同一學生帳號。

## 資料隔離

每位教師使用一套獨立 Worker 與 D1 資料庫。請勿把自己的 D1 Database ID 手動填入他人的部署設定，否則可能造成資料共用。
